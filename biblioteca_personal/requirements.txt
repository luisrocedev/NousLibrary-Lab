# ═════════════════════════════════════════════════════════════
#  NousLibrary-Lab - Dependencias del proyecto
# ═════════════════════════════════════════════════════════════

# Librerías estándar utilizadas (incluidas en Python 3.10+):
# dataclasses, datetime, json, csv, xml.etree.ElementTree,
# sqlite3, logging, pathlib, typing, abc, os, tkinter, hashlib, hmac

# ──── API REST ────
Flask>=3.0.0
Flask-CORS>=4.0.0

# ──── Autenticación JWT ────
PyJWT>=2.8.0

# ──── Validación de datos ────
jsonschema>=4.19.0

# ──── Parsing XML avanzado (opcional) ────
lxml>=4.9.0